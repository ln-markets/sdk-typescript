const config = {
  arrowParens: 'always',
  printWidth: 80,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
  useTabs: false,
}

// oxlint-disable-next-line no-default-export -- This is how prettier works
export default config
